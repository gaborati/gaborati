name: Update LeetCode Stats
 
on:
  schedule:
    # Runs every day at 03:00 UTC
    - cron: "0 3 * * *"
  workflow_dispatch: {} # allows manual runs from the GitHub Actions tab
 
permissions:
  contents: write
 
jobs:
  update-stats:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
 
      - name: Fix SSL certificates
        run: |
          sudo apt-get update
          sudo apt-get install --reinstall -y ca-certificates
          sudo update-ca-certificates
          git config --global http.sslCAInfo /etc/ssl/certs/ca-certificates.crt
 
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
 
      - name: Fetch stats and generate card
        env:
          LEETCODE_USERNAME: aaa96
        run: node .github/workflows/scripts/updateLeetcode.js
 
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md leetcode-card.svg
          git diff --cached --quiet && echo "No changes" || git commit -m "chore: update LeetCode stats card"
          git push
 
